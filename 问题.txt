- 流量记录异常分数计算逻辑是什么，为什么看到的分数全为0，计算逻辑是否可以优化，用什么方式优化，是否需要接入ai

- 流量记录到安全告警的逻辑是什么
- 流量记录中评分均为低分，为什么也会产生安全告警
- 安全告警中的告警类型是根据什么特征判断出来的
- 安全告警ai分析逻辑是什么，是否可以优化，是否需要接入ai的api并对ai进行针对性训练
- 安全告警研判置信度是否为真实数据，如何计算的
- 安全告警中有ai自动填入的构建和需要用户点击的编译计划，二者有什么区别？为什么有些告警点击编译计划无响应？为什么二者创建的行动计划不一致？为什么二者dry-run后的预测效果不一致？是否可以删去一部分？
- 安全告警ai自动填入的构建动作是有依据的还是自己猜的，为什么修改动作（增加几项动作）之后进行编译计划，结构化动作没有变化，和修改动作之前一样？
- 安全告警中威胁上下文的作用是什么，逻辑是什么，是否可以汉化，威胁上下文是否对应alert匹配，如何对应


- dryrun推演的指标是真实的还是模拟的，计算逻辑是什么
- 现有的pcap包分析之后，告警类型全为anomaly
- 构建动作后，拓扑图看起来没有变化，只是多加了一个dry——run影响框，节点和边的变化没有体现
- 拓扑图检查器信息是否可以更丰富一些有用信息
- 拓扑图节点数量少是pcap包的问题吗？DAG、力导向、圆形等有什么区别和作用。


- 回归测试删除支持
- 场景测试场景失败含义，检查项是否可以添加
- 创建场景的功能是否可以优化
- 场景回归中processing time显示为-
- 场景回归并没有体现实时以及更可观测

- 内部事件总线是否有必要接入redis
- 这套“统一走 EventBus”的设计基本成立，但不是百分之百纯。broadcast_scenario_done() 在 publish 到 EventBus 之后，又直接调用了一次 manager.broadcast()，这是一条绕过总线的旁路。
- 项目从头到尾各个流程中，哪部分可以接入ai、进行算法升级等

- 态势评分
- 汽车仪表盘
- 最近活动限制高度
- 拓扑图视角问题

