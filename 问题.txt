mock和real的区别，后续应用时采用哪种模式，还是两种模式混用，如果只采用real模式，mock相关文件需要全部删除吗
如果要切换到real模式，需要修改哪些文件，我需要提供哪些东西
real模式的时候，agent会对不同的pcap进行针对性分析吗，分析之后alert数据的变化也是对不同的pcap针对性的吗
这个项目的作用是什么，是做什么的
pcap是怎么到alert的
用户手册
中英文切换
agent如何实现的，能否继续升级，需不需要大模型api，需要的话需不需要针对性训练
每个页面的作用
界面美化
特征 -> 流量 -> 告警 -> 研判 -> 动作 -> 推演的流程是什么，哪一步有ai介入



前端存在需修复的问题
- real.ts envelope 解包缺失 — fetchJson 直接返回原始响应，未提取 .data，联调时所有数据将无法正确使用
- real.ts getPcapStatus 路径错误 — 应为 /pcaps/${id}/status，实际是 /pcaps/${id}
- WebSocket real 模式未连接 — 缺少全局 wsClient.connect() 调用入口
- types.ts 多处字段名与 DOC C 不一致 — PcapFile.size vs size_bytes、Alert 缺少 aggregation/agent/twin、Scenario 结构扁平化、version 字段普遍缺失
- twin.dryrun.created WS 事件未监听
- FlowFilterBar 缺少 pcap_id 和 time range 筛选；FlowTable 缺少 duration 列
- TopologyToolbar 缺少 start/end 时间输入
- AlertDetail组件功能嵌入 page.tsx，非独立文件
- docker-compose frontend 被注释,docker-compose.yml需取消注释并配置 frontend 构建